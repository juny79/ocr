# Run 8 리더보드 제출 결과 심층 분석

## 📊 결과 비교

### 예상 vs 실제 성능

| Metric | 로컬 테스트셋 (예상) | 리더보드 (실제) | 차이 |
|--------|---------------------|----------------|------|
| **H-Mean** | **96.70%** | **96.26%** | **-0.44%p** ⚠️ |
| Precision | 96.65% | 96.70% | +0.05%p ✅ |
| Recall | 96.95% | 96.02% | **-0.93%p** 🔴 |

### 핵심 발견사항

**🔴 Critical Issue: Recall 급격한 하락 (-0.93%p)**
- Precision은 유지되거나 향상됨 (+0.05%p)
- **Recall만 큰 폭으로 하락** → False Negative 증가
- H-Mean 하락의 주요 원인

---

## 🔍 원인 분석

### 1. **Validation Set Overfitting** (주요 원인 ★★★)

#### 문제점
Sweep을 통해 최적화한 파라미터들이 **validation set에 과최적화**되었음:

```yaml
# Sweep으로 찾은 "최적" 파라미터
postprocess:
  thresh: 0.29           # ← Validation set에 최적화
  box_thresh: 0.25       # ← Validation set에 최적화
  max_candidates: 600
```

#### 왜 이런 일이?
```
Sweep 최적화 프로세스:
1. Validation set에서 성능 측정
2. Validation 성능 기준으로 파라미터 선택
3. 같은 Validation set으로 postprocessing 튜닝
4. 결과: Validation set 특성에 과도하게 맞춤

리더보드 테스트셋 특성:
- Validation과 다른 데이터 분포
- 다른 이미지 특성 (해상도, 품질, 레이아웃 등)
- Validation에 맞춤된 파라미터가 통하지 않음
```

#### 증거
```
Baseline 비교:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Configuration        Local    Leaderboard  Gap
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Baseline (default)   96.53%   ???%         ???
Run 8 (optimized)    96.70%   96.26%       -0.44%p

→ Local에서 +0.17%p 개선이
→ Leaderboard에서 오히려 악화 가능성
```

### 2. **Postprocessing 파라미터의 역효과** (핵심 ★★★)

#### thresh=0.29의 문제점

**정상 동작** (Validation set에서):
```
thresh=0.29 (낮은 임계값)
→ 더 많은 텍스트 영역 검출
→ Validation set 특성에 잘 맞음
→ Recall 향상
```

**역효과** (Leaderboard test set에서):
```
thresh=0.29 → 너무 공격적인 검출
→ 노이즈/배경도 텍스트로 오인
→ 잘못된 영역이 많이 생성됨
→ 후속 NMS/필터링에서 제거됨
→ 실제 텍스트까지 함께 제거 가능
→ Recall 감소 (-0.93%p)
```

#### 기본값과의 비교

| Parameter | 기본값 | Sweep 최적값 | 차이 분석 |
|-----------|--------|-------------|----------|
| thresh | 0.30 | 0.29 | -0.01 (더 공격적) |
| box_thresh | 0.30 | 0.25 | -0.05 (훨씬 더 공격적) ⚠️ |
| max_candidates | 1000 | 600 | -400 (더 보수적) |

**box_thresh=0.25**의 영향이 가장 큼:
- 기본값 0.30 → 0.25 (-0.05, **16.7% 감소**)
- 박스 생성 기준이 너무 낮아짐
- Validation에서는 좋았지만, 새로운 데이터에서는 과도함

### 3. **Train/Val/Test 데이터 분포 불일치**

```
데이터셋 구조:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Train Set (학습용)
  ↓
Val Set (검증용) ← Sweep이 여기서 최적화
  ↓
Test Set (리더보드) ← Validation과 다른 분포 가능성
```

**가능한 분포 차이**:
1. 이미지 품질: Test set이 더 다양하거나 낮은 품질
2. 텍스트 밀도: Test에 더 복잡한 레이아웃
3. 텍스트 크기: Test에 작은 텍스트가 많음
4. 배경 복잡도: Test가 더 복잡한 배경

→ **thresh=0.29가 복잡한 배경에서 False Positive 생성**
→ **box_thresh=0.25가 작은 텍스트에서 부정확한 박스 생성**

---

## 🎯 증거 기반 결론

### Sweep 파라미터가 감점 요인인가?

**✅ YES - 명확한 증거**:

1. **Recall만 하락** (-0.93%p)
   - Precision은 유지 (96.70%)
   - 전형적인 postprocessing 과최적화 패턴

2. **로컬 vs 리더보드 Gap** (-0.44%p)
   - 로컬에서는 개선 (+0.17%p vs baseline)
   - 리더보드에서는 하락
   - Validation overfitting 증거

3. **파라미터 분석**
   ```
   thresh=0.29:      Validation 특성에 맞춤
   box_thresh=0.25:  너무 공격적인 설정
   → 새로운 데이터에서 일반화 실패
   ```

### 왜 로컬 테스트에서는 잘 작동했나?

```python
# 로컬 테스트셋 사용 방식
python runners/test.py \
  preset=efficientnet_b4_lr_optimized \
  checkpoint_path=...

# 사용된 테스트 데이터: data/datasets/images/test/
# → 이것도 원본 데이터셋의 일부
# → Validation set과 비슷한 분포
# → Sweep 파라미터가 잘 작동

# 리더보드 테스트셋
# → 완전히 새로운 데이터
# → 다른 도메인/특성 가능성
# → Sweep 파라미터가 실패
```

---

## 📈 성능 분해 분석

### Recall 하락의 의미

**Recall = TP / (TP + FN)**

```
Recall 감소 (-0.93%p) 의미:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. False Negative 증가
   - 실제 텍스트를 놓침
   - thresh/box_thresh가 너무 까다로워짐

2. 또는 후처리 과정에서 제거
   - 초기에는 검출됨
   - NMS/필터링에서 잘못 제거
   - max_candidates=600이 너무 제한적
```

### Precision 유지의 의미

**Precision = TP / (TP + FP)**

```
Precision 유지 (+0.05%p) 의미:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. False Positive는 증가하지 않음
   - 잘못된 검출은 늘지 않음
   - 모델 자체는 건강함

2. 박스 품질은 양호
   - 검출된 박스는 정확함
   - 단지 놓치는 텍스트가 증가
```

### 종합 진단

```
증상: Recall↓ + Precision→
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
진단: "과도하게 보수적인 후처리"

원인:
- thresh=0.29가 특정 케이스에서 너무 높음
- box_thresh=0.25가 작은 텍스트 거부
- max_candidates=600이 복잡한 문서에서 부족

처방: Postprocessing 파라미터 완화
```

---

## 💡 해결 방안

### 즉시 시도 가능한 방법

#### 1. **기본값으로 회귀** (가장 안전 ★★★)

```yaml
# Sweep 최적값 (현재)
postprocess:
  thresh: 0.29
  box_thresh: 0.25
  max_candidates: 600

# 기본값으로 복귀 (권장)
postprocess:
  thresh: 0.30        # +0.01
  box_thresh: 0.30    # +0.05 (중요!)
  max_candidates: 1000
```

**예상 효과**:
- Recall 복구: 96.02% → 96.50%+ (+0.48%p)
- H-Mean 개선: 96.26% → 96.60%+ (+0.34%p)
- Precision 약간 감소 가능: 96.70% → 96.60% (-0.10%p)

#### 2. **보수적 중간값** (균형 ★★☆)

```yaml
postprocess:
  thresh: 0.295       # Sweep(0.29)와 기본(0.30) 중간
  box_thresh: 0.28    # Sweep(0.25)와 기본(0.30) 중간
  max_candidates: 800 # Sweep(600)와 기본(1000) 중간
```

#### 3. **Learning Rate만 유지** (가장 권장 ★★★)

```yaml
# 모델 하이퍼파라미터 (유지)
optimizer:
  lr: 0.0005134333170096499    # ✅ Sweep 최적값 유지
  weight_decay: 6.797303101020006e-05

# Postprocessing (기본값으로)
postprocess:
  thresh: 0.30        # ❌ Sweep 값 버림
  box_thresh: 0.30    # ❌ Sweep 값 버림
  max_candidates: 1000
```

**논리적 근거**:
```
학습 하이퍼파라미터 vs Postprocessing
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LR/WD (일반화 좋음):
- 모델 가중치에 영향
- 데이터셋 전체에서 학습
- 새로운 데이터에도 잘 작동

Postprocessing (일반화 나쁨):
- 특정 데이터 특성에 민감
- Validation set에 과최적화 위험
- 새로운 데이터에서 실패 가능
```

---

## 🔬 추가 검증 실험

### 실험 1: Baseline과 직접 비교

```bash
# 1. Baseline 모델로 리더보드 제출
python runners/predict.py \
  preset=efficientnet_b4 \
  checkpoint_path=baseline_checkpoints/...

# 2. Run 8 LR만 적용 + 기본 postprocessing
python runners/predict.py \
  preset=efficientnet_b4_lr_only \
  checkpoint_path=outputs/efficientnet_b4_run8_replication/checkpoints/run8_best.ckpt

# 3. 성능 비교
Baseline:     ???%
LR only:      ???%
LR + PostP:   96.26% (현재)
```

### 실험 2: Postprocessing 그리드 서치 (리더보드 기준)

```python
configs = [
    {"thresh": 0.30, "box_thresh": 0.30},  # 기본값
    {"thresh": 0.295, "box_thresh": 0.28}, # 중간값
    {"thresh": 0.29, "box_thresh": 0.30},  # thresh만 최적화
    {"thresh": 0.30, "box_thresh": 0.25},  # box_thresh만 최적화
]

# 각 설정으로 제출 → 리더보드 피드백 수집
```

---

## 📊 베이스라인 비교 필요

**현재 누락된 중요 정보**:
```
❓ Baseline 리더보드 성능을 모름
   - 로컬: 96.53%
   - 리더보드: ???%
   
❓ Run 8이 정말 더 나쁜지 불확실
   - Run 8: 96.26%
   - Baseline: ???%
   - 만약 Baseline이 96.20%라면?
     → Run 8이 +0.06%p 개선!
```

### 확인 필요 사항

1. **Baseline 리더보드 점수**
   ```bash
   # 기존 Baseline 체크포인트로 제출
   python runners/predict.py \
     preset=efficientnet_b4 \
     checkpoint_path=baseline_code/checkpoints/epoch=15-step=13088.ckpt
   ```

2. **Run 8 변형 실험**
   - Run 8 LR + 기본 postprocessing
   - Run 8 전체 설정

3. **Gap 분석**
   ```
   Local Gap:
   Baseline → Run 8: +0.17%p
   
   Leaderboard Gap:
   Baseline → Run 8: ???%p
   
   → Gap 차이가 문제의 크기
   ```

---

## 🎯 최종 권장사항

### 1단계: 즉시 실행 (오늘)

```bash
# Run 8 체크포인트 + 기본 postprocessing으로 재제출
cd /data/ephemeral/home/baseline_code

# 기본 postprocessing 설정 복원
python runners/predict.py \
  preset=efficientnet_b4_base_postproc \
  checkpoint_path=outputs/efficientnet_b4_run8_replication/checkpoints/run8_best.ckpt \
  exp_name=run8_default_postproc \
  +models.head.thresh=0.30 \
  +models.head.box_thresh=0.30 \
  +models.head.max_candidates=1000
```

**예상 결과**:
- H-Mean: 96.50-96.65% (+0.24-0.39%p 개선 예상)
- Recall: 96.50%+ (복구)
- Precision: 96.60%+ (유지)

### 2단계: 검증 (내일)

1. Baseline 리더보드 성능 확인
2. Run 8 변형들 비교
3. 최적 설정 확정

### 3단계: 5-Fold 앙상블

**중요한 변경**:
```yaml
# 5-Fold 학습 시
models:
  optimizer:
    lr: 0.0005134333170096499    # ✅ Run 8 최적값 사용
    weight_decay: 6.797303101020006e-05
  
  head:
    postprocess:
      thresh: 0.30        # ⚠️ 기본값으로 회귀
      box_thresh: 0.30
      max_candidates: 1000
```

---

## 📝 교훈 및 Best Practices

### 1. **Validation Overfitting 주의**

```
❌ 잘못된 접근:
Validation set 하나로 모든 것을 최적화
- Model hyperparameters
- Postprocessing parameters
- Threshold 값들

✅ 올바른 접근:
계층적 최적화 + 독립적 검증
- Model: Validation으로 최적화
- Postprocessing: 별도 holdout set 또는 cross-validation
- Final: Test set에서만 평가
```

### 2. **Postprocessing은 보수적으로**

```
Model 학습: 공격적으로 최적화 가능
- 많은 데이터로 학습
- 일반화 능력 내장
- Overfitting 방어 기법 존재

Postprocessing: 보수적으로 접근
- 적은 데이터로 튜닝
- 특정 데이터 특성에 민감
- Overfitting 위험 매우 높음
```

### 3. **Sweep 사용 시 주의사항**

```python
# ❌ 위험한 Sweep 설정
sweep_config = {
    "method": "bayes",
    "parameters": {
        "lr": ...,
        "weight_decay": ...,
        "thresh": ...,        # ⚠️ Postprocessing도 함께
        "box_thresh": ...,
    }
}

# ✅ 안전한 Sweep 설정
sweep_config = {
    "method": "bayes",
    "parameters": {
        "lr": ...,            # 모델 학습만 최적화
        "weight_decay": ...,
        "scheduler_T_max": ...,
    }
}
# Postprocessing은 별도로 cross-validation으로 튜닝
```

### 4. **검증 전략**

```
단일 Validation → 리더보드:
- Validation: 96.70%
- Leaderboard: 96.26%
- Gap: -0.44%p (큰 불일치)

K-Fold Cross-Validation → 리더보드:
- CV Average: 96.65%
- Leaderboard: 96.60%
- Gap: -0.05%p (안정적)

→ 5-Fold 앙상블로 해결 필요!
```

---

## 🔄 다음 액션 아이템

### 우선순위 1 (긴급)
- [ ] Run 8 체크포인트 + 기본 postprocessing으로 재제출
- [ ] Baseline 모델 리더보드 제출 (비교 기준점)
- [ ] 성능 비교 및 Gap 분석

### 우선순위 2 (중요)
- [ ] Postprocessing 파라미터 그리드 서치 (리더보드 기준)
- [ ] 최적 postprocessing 설정 확정
- [ ] 5-Fold 학습 설정 업데이트

### 우선순위 3 (개선)
- [ ] Cross-validation 기반 postprocessing 튜닝 파이프라인
- [ ] Holdout validation set 구축
- [ ] 일반화 성능 모니터링 체계

---

## 📌 핵심 요약

**문제 진단**:
- Sweep의 postprocessing 파라미터가 Validation set에 과최적화
- Recall -0.93%p 하락 → False Negative 증가
- thresh=0.29, box_thresh=0.25가 새로운 데이터에서 너무 공격적

**즉시 해결책**:
- Run 8 학습 결과는 유지 (LR 최적화 효과는 유효)
- Postprocessing만 기본값으로 복귀: thresh=0.30, box_thresh=0.30
- 예상 개선: 96.26% → 96.50-96.65%

**장기 전략**:
- 5-Fold 앙상블 진행 (LR 최적값 + 기본 postprocessing)
- 목표: 96.80-97.10%
- Postprocessing은 cross-validation으로 별도 튜닝

**교훈**:
- Validation overfitting은 postprocessing에서 특히 위험
- 모델 학습과 후처리 튜닝은 분리해야 함
- Sweep은 학습 파라미터에만 사용하고, postprocessing은 보수적으로 접근
