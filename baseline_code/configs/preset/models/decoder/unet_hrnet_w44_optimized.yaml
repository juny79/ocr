# @package _global_

# Optimized UNet Decoder for HRNet-W44
# Based on 0.9886 score configuration
# Decoder Channels: [128, 256, 512, 1024]

models:
  decoder:
    _target_: ${decoder_path}.UNet
    in_channels: [128, 256, 512, 1024]  # HRNet-W44 feature channels (optimized)
    strides: [4, 8, 16, 32]
    inner_channels: 256
    output_channels: 64
    bias: False
